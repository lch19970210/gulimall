# 数据源相关信息
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://115.29.212.18:3306/gulimall_ums?useUnicode=true&characterEncoding=utf-8
    driver-class-name: com.mysql.cj.jdbc.Driver
  cloud:
    # nacos注册中心地址
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
  application:
      name: gulimall-member

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
server:
  port: 8000