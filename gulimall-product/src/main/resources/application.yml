# 数据源相关信息
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.176.128:3306/gulimall_pms?useUnicode=true&characterEncoding=utf-8
    driver-class-name: com.mysql.cj.jdbc.Driver
  application:
    name: gulimall-product
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
      # 逻辑删除赋值(可有可无)
      logic-delete-value: 1
      logic-not-delete-value: 0


server:
  port: 10000
logging:
  level:
    com.hangzhou.gulimall: debug